{
  "api_key": {
    "type": "securestring",
    "uiDefinition": {
      "displayName": "ZendeskAuthToken",
      "description": "Your Zendesk authorization token. Use 'Basic <base64(email/token:api_token)>' for API token auth.",
      "tooltip": "Provide your Zendesk authorization token",
      "constraints": {
        "tabIndex": 2,
        "clearText": false,
        "required": "true"
      }
    }
  }
}
