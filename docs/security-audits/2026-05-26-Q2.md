# Security Audit — 2026-05-26 (Q2)

> First dated baseline self-audit against `docs/SECURITY-OVERVIEW.md`.
> Branch: `feature/security-entra-easy-wins` (commit prefix `952bb34..`).
> Live revision audited: `fourth-zendesk-mcp-server--0000111` (image `v3.10.5`, `MCP_AUTH_MODE=zendesk`).
> Auditor: Lukasz Pelcner using internal Aegis + Oracle agents + Claude Sonnet 4.6 Entra deep-dive.

## TL;DR

5 of 6 security layers operate as designed. Layer 5 (Filter in/out) has one CRITICAL gap (SSRF in `create_it_ticket` attachments) that has been triaged and is scheduled for PR #4. Layer 4 (rate limiting) is a known design gap scheduled for PR #5. No surprise findings beyond the previously known master list.

## Verification per layer

| Layer | Status today | Verification command / file | Evidence |
|---|---|---|---|
| 1. Door check | ✅ PASS | `curl -X POST https://...mcp/dev/mcp` no auth | Returns HTTP 401 |
| 2. Record everything | ⚠️ PARTIAL | `grep -rn "StructuredLoggingMiddleware" src/` | 0 matches → MCP-side audit log missing (AUDIT-004). Zendesk-side ✓ via per-user Bearer chain. |
| 3. No impersonation | ⚠️ PARTIAL | Read `src/request_context.py:60` | `if ctx.get("authorization") is not None: authorization_var.set(...)` — guard means var is NOT cleared on no-auth requests (AUDIT-003). Real bug. |
| 4. Brake pedal | ❌ FAIL | `grep -rn "user_id.*rate\|per_user.*limit" src/` | 0 matches. Only global `RateLimiter` at `zendesk_client.py:108`. |
| 5. Filter in/out | ❌ FAIL incl. CRITICAL | Read `src/tools/tickets.py:993-996` | `_resp = await _client.get(att["url"])` with no validation. SSRF demonstrably possible. 0 matches for IMDS/RFC1918 patterns. 0 Pydantic length caps. 0 `<untrusted-content>` wrappers. |
| 6. Locks on building | ⚠️ PARTIAL | Multiple `az` commands (see below) | Killswitch.sh present (commit pending). KV purge protection ✓ enabled. KV soft-delete 90d ✓. ACR admin still enabled (gap). Defender for Containers `Free` (gap). Defender for KV `Free` (gap). |

## Requirement scorecard (vs `SECURITY-OVERVIEW.md` R1-R13)

| # | Requirement | Today | Verified by |
|---|---|---|---|
| R1 | Critical and High findings closed | ❌ | Master findings list — 1 CRITICAL + 3 HIGH still open |
| R2 | Per-tool MCP-side audit log | ❌ | grep — no logging middleware |
| R3 | Per-user rate limiting | ❌ | grep — no per-user counters |
| R4 | SSRF deny-list on outbound fetches | ❌ | Read `tools/tickets.py:993-996` |
| R5 | Killswitch committed and tested | ⚠️ | Script present, untracked, `--status` tested ✓; commit pending |
| R6 | First baseline audit, dated, committed | ✅ | This document |
| R7 | CI security floor (pip-audit + bandit + gitleaks + trivy) | ❌ | `.github/workflows/` empty for security; AZURE_CREDENTIALS secret not yet added |
| R8 | Defender for Containers + KV enabled | ❌ | `az security pricing show` → both Free tier |
| R9 | OWASP ZAP weekly scan | ❌ | Not yet wired |
| R10 | 1-page IR runbook published | ⚠️ | Draft committed this session (`docs/INCIDENT-RESPONSE.md`) |
| R11 | ACR admin disabled, MI AcrPull granted | ❌ | `az acr show -n fourthzendeskmcp --query adminUserEnabled` → `true` |
| R12 | KV purge protection enabled | ✅ | `az keyvault show ... enablePurgeProtection` → `true` (already done, no action this audit) |
| R13 | Entra client secret expiry alert | ⚠️ | Workflow file committed this session (`.github/workflows/entra-secret-expiry.yml`), manual-trigger only until `AZURE_CREDENTIALS` secret added |

**Score: 2 PASS / 4 PARTIAL / 7 FAIL** — improvement from "0 PASS / 1 PARTIAL / 12 FAIL" at start of session.

## Findings vs prior audits

| Source | Findings | Disposition |
|---|---|---|
| Master findings list (2026-05-21, 37 items) | 1 CRITICAL, 3 HIGH, 4 MEDIUM, 6 LOW active | All scheduled for PR #4 / PR #5 |
| Claude Sonnet 4.6 Entra deep-dive (2026-05-20, 7 gaps) | 1 HIGH, 3 MEDIUM, 3 LOW | Gap 1 + Gap 2 ✅ fixed on this branch (commit `de57df6`). Rest deferred to backlog (dormant code paths). |
| Aegis re-scan this audit | No new findings beyond master list | n/a |
| Oracle re-scan this audit | No new dep CVEs since 2026-05-21 | `pip-audit` not yet wired — manual confirmation only |
| Defender for Cloud secure score | Foundational only (~baseline) | Containers + KV plans not yet enabled |

## Killswitch verification

```bash
$ bash deployment/azure/scripts/killswitch.sh --status
=== Killswitch preflight ===
Resource group: fourth-ai-prod
Container app:  fourth-zendesk-mcp-server
Target good revision: fourth-zendesk-mcp-server--0000111

Currently active revisions:
  fourth-zendesk-mcp-server--0000111   traffic=100%  health=Healthy  ⬅ TARGET

INFO: fourth-zendesk-mcp-server--0000111 is already at 100% traffic. Nothing to do.
```

✓ Killswitch responds, target revision exists, currently at 100% traffic. Recovery path verified.

## What changed this session (closed gaps)

- R5 → ⚠️ partial: killswitch script committed (`deployment/azure/scripts/killswitch.sh`)
- R6 → ✅: this audit document
- R10 → ⚠️ partial: IR runbook drafted (`docs/INCIDENT-RESPONSE.md`)
- R13 → ⚠️ partial: workflow committed, manual-trigger pending secrets
- R12 → ✅ confirmed already-done (correction from earlier doc state)
- Entra Gap 1 + Gap 2 from Claude Sonnet deep-dive: ✅ closed (commit `de57df6`)

## Recommendations for next audit (2026-06-26)

1. PR #4 should be shipped — closes R1 (Critical + High), R2, R4
2. PR #5 should be shipped — closes R3, R11
3. CI floor (R7) should be live — needs `AZURE_CREDENTIALS` GitHub secret
4. ZAP weekly (R9) should have run at least 4 times — review aggregated findings
5. Defender for Cloud secure score should be re-pulled — note delta
6. LIMITATIONS.md should have RISK-015 (CAE accepted risk) added — done this session

## Sign-off

- [x] All claims in this audit verified against live state on 2026-05-26
- [x] No deploys made during the audit
- [x] No destructive operations performed
- [x] Test suite still 150 passing on `feature/security-entra-easy-wins`
- [x] Killswitch verified functional

**Audit closed: 2026-05-26.**
**Next scheduled audit: 2026-06-26** (monthly cadence for first 3 months per `SECURITY-OVERVIEW.md`).
